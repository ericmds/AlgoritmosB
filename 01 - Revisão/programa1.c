#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
  //system("cls");      Limpar a tela no linux - clear
    system("clear"); // Limpar a tela no windows - cls
    printf("Boa noite tuma\n");

    return 1;
}

// Mostrando os arquivos    | ls
// Abrindo arquivos         | cd nomeArquivo
// Compilando um arquivo .c | gcc nomeArquivo.c -o roda
// Executando esse arquivo  | ./roda
